/** Padding horizontal consistente das páginas autenticadas (mobile-first). */
export const paddingPaginaShell = {
  px: { xs: 1, sm: 2, md: 3, lg: 4 },
  pt: { xs: 1.5, sm: 2 },
  pb: { xs: 3, sm: 4 },
} as const;

export const larguraConteudoPagina = {
  width: '100%',
  maxWidth: '100%',
  boxSizing: 'border-box' as const,
} as const;

export const paddingPaginaDetalhe = {
  p: { xs: 1, sm: 2, md: 3 },
} as const;

export const paddingPaginaFormulario = {
  p: { xs: 1, sm: 2, md: 3 },
} as const;
